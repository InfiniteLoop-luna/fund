# Streamlit Secrets Template
#
# 使用说明:
# 1. 复制此文件为 secrets.toml
# 2. 将下面的 token 替换为你的真实 Tushare token
# 3. secrets.toml 已在 .gitignore 中，不会被提交到 git
#
# 获取 Tushare token:
# 访问 https://tushare.pro/register 注册并获取 token

[tushare]
token = "your_tushare_token_here"
